import * as React from 'react';
import './style.css';
interface Props {
    title?: string;
    url?: string;
    children: React.ReactNode;
}
declare const _default: ({ title, url, children }: Props) => JSX.Element;
export default _default;
